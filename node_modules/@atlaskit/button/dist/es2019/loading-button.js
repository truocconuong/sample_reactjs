import React from 'react';
import Button from './button';
import LoadingSpinner from './shared/loading-spinner';
const LoadingButton = React.forwardRef(function LoadingButton({ isLoading = false, ...rest }, ref) {
    // Button already has React.memo, so just leaning on that
    return (React.createElement(Button, Object.assign({}, rest, { ref: ref, overlay: isLoading ? React.createElement(LoadingSpinner, Object.assign({}, rest)) : null })));
});
// Tools including enzyme rely on components having a display name
LoadingButton.displayName = 'LoadingButton';
export default LoadingButton;
//# sourceMappingURL=loading-button.js.map